<!DOCTYPE html>
<html style="width:100%; height:100%">
<head>
    <% include partials/head.ejs %>

    <style>
        .my-line {
            fill: none;
            stroke-linejoin: round;
            stroke-linecap: round;
            stroke-width: 3;
        }

        .dot:hover {
            r: 10px;
        }

        .tooltip {
            position: absolute;
            text-align: center;
            padding: 10px;
            background: #f54;
            color: black;
            pointer-events: none;
            border-radius: 5px;
            border: 1px solid black;
        }

        .x-button {
            font-size: 40px;
            position: absolute; 
            right: 10px; 
            top: 0px; 
        }
        .x-button:hover {
            color: gray;
        }

        .chart-container {
            position: relative; 
            width: 45%;
            margin: 2%;
            float: left; 
            border: 1px solid black; 
            border-radius: 15px;
            /*-moz-transition: all 3s;
            -webkit-transition: all 3s;
            -o-transition: all 3s;*/
        }
    </style>
</head>

<body style="width:100%; height:100%">
    <h1>HELLO</h1>

    <script type=text/javascript src="test.js"></script>
    <script>
        function removeGraph(button) {
            var graph = $(button.parentNode)
            setTimeout(function() {
                graph.remove();
            }, 2000);

            graph.fadeOut(500, function() {
                graph.css({"visibility": "hidden", "display": "block"}).animate({width:"toggle"}, 500);
            });

            // setTimeout(function() {
            //     d3.select(button.parentNode).remove();
            // }, 3001)

            // d3.select(button.parentNode)
            //     .style("width", 0)
            //     .style("opacity","0");

            // console.log("im sane")
        }
        
        d3.json("/api/course/CMSC420", function(data) {
            genChart(data['CMSC420'], 'CMSC420');
        })

        d3.json("/api/course/CMSC411", function(data) {
            genChart(data['CMSC411'], 'CMSC411');
        })

        d3.json("/api/course/CMSC412", function(data) {
            genChart(data['CMSC412'], 'CMSC412');
        })

        d3.json("/api/course/CMSC430", function(data) {
            genChart(data['CMSC430'], 'CMSC430');
        })

    </script>
</body>
</html>